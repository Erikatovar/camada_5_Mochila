Crear Repositorio Local
$git init

Subir los archivos al repositorio local
$git add .

Hacer commit de los cambios en repositorio local
$git commit -m (descripcion de los cambios)

Subir los cambio al repositorio remoto
$git push -u origin main

Conectar mi repositorio local con algun repositorio remoto de github
$git remote add origin (url del repositorio remoto en github)

Ver el estados de los archivos en el repositorio local
$git status

Ver el historial de commit realizados en el repositorio local
$git log
  
Ver las ramas
$git branch

Crear ramas
$git branch (nombredelarama)

Moverme entre ramas
$git checkout (nombredelarama) o $git switch (nombredelarama)

Clonar un repositorio remoto en nuestra compu
$git clone (URL del repositorio remoto)

Actualizar los cambios del repositorio remoto en mi repositorio local
$git pull

